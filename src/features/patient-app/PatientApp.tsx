import { ScreenStub } from '@/components/ScreenStub';

export default function PatientApp() {
  return <ScreenStub title="Patient companion" meta="Priya Deshmukh" milestone={8} scope="Chat with the clinic and the “My journey” view, inside a phone frame with a Marathi / English toggle." />;
}
