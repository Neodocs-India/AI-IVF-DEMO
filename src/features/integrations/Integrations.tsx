import { ScreenStub } from '@/components/ScreenStub';

export default function Integrations() {
  return <ScreenStub title="Integrations" milestone={11} scope="How Anvaya reads from the clinic's existing IVF software, EMR, lab, billing, telephony and WhatsApp." />;
}
