import { ScreenStub } from '@/components/ScreenStub';

export default function Directory() {
  return <ScreenStub title="Patients" milestone={5} scope="Searchable table of every patient, linking to each Patient 360 record." />;
}
