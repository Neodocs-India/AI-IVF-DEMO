import { ScreenStub } from '@/components/ScreenStub';

export default function CryoRegister() {
  return <ScreenStub title="Cryo-Storage Register" milestone={9} scope="Storage summary, filterable register, unreachable-patient protocol and tank map." />;
}
