import { ScreenStub } from '@/components/ScreenStub';

export default function FollowUpRules() {
  return <ScreenStub title="Follow-up Rules" milestone={11} scope="The 14 follow-up rules with thresholds, owners, escalation and sensitive flags." />;
}
